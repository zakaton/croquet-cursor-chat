import WidgetModel from './WidgetModel.js'

class AudioWidgetModel extends WidgetModel {
    init (options) {
        super.init(options)
    }
}
AudioWidgetModel.register('audio-widget')

export default AudioWidgetModel