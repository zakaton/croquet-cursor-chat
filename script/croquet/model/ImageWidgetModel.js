import WidgetModel from './WidgetModel.js'

class ImageWidgetModel extends WidgetModel {
    init (options) {
        super.init(options)
    }
}
ImageWidgetModel.register('image-widget')

export default ImageWidgetModel